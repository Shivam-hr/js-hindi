console.log(null > 0);

console.log(null == 0);

console.log(null >= 0);

console.log(undefined > 0); //always false in every case
console.log(undefined == 0);
console.log(undefined >= 0);  


// strict check (===) it comapre both value and data type so it is a strict check 

// Equaility Operator and comparison operator both work differently. 
// bcoz comparison operator convert null number to 0. And after that start comparison.


